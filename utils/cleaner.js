const data = require('./albums.json')
const fs = require('fs')

// const parsedData = data.map(album => {
//   console.log(album)
//   // return JSON.parse(album)
// })

console.log(data.length)

// fs.writeFileSync('cleanData.json', parsedData)
